# 배포 검증 체크리스트

2026-09-10 기준. `자동 통과`는 저장소의 실제 최종 함수 또는 배포 파일을 대상으로 한 회귀 테스트이며, `실기기 대기`는 게시 완료된 Firebase 소유자 규칙으로 두 브라우저/실기기에서 마쳐야 하는 항목이다.

| 검증 항목 | 상태 | 현재 근거 |
| --- | --- | --- |
| PC 세션 A에서 만든 업무가 세션 B에 나타난다 | 실기기 대기 | 소유자 UID 고정 Rules 게시 완료, 동일 Google 계정의 두 세션에서 실행 |
| 상태 변경이 다른 세션에 실시간 반영된다 | 실기기 대기 | 컬렉션별 `onSnapshot` 구현·계약 테스트 통과 |
| 체크리스트 체크/해제가 다른 세션에 반영된다 | 실기기 대기 | 체크항목 독립 문서 및 문서 단위 diff 테스트 통과 |
| 1단계 시작일 변경 시 기존 2·3·4단계 일정이 재계산된다 | 자동 통과 | 2026-09-04→09-08 이동 후 2·3·4단계가 09-09, 09-11, 09-14로 재계산됨 |
| 날짜 변경으로 같은 업무가 중복 생성되지 않는다 | 자동 통과 | 기존 네 개 ID와 건수가 재계산 전후 동일 |
| 같은 `generatedKey`의 자동업무가 한 건만 존재한다 | 자동 통과 | 같은 범위 두 번 동기화 시 두 번째 생성 0건, 완료 이력 우선 중복 복구 통과 |
| 변경 직후 간트와 달력이 함께 갱신된다 | 자동 통과 / 화면 확인 대기 | 날짜 변경 경로의 목록·HOME 재렌더와 간트·달력 동시 렌더 계약 통과 |
| 간트의 1~12개월 범위와 반복주기 토글이 함께 적용된다 | 자동 통과 / 화면 확인 대기 | ±3개월 경계와 주간 숨김을 조합하고, 주간 연계단계까지 제외·복원하는 회귀검사 통과 |
| HOME 조작부가 사용 대상에 맞게 배치된다 | 자동 통과 / 화면 확인 대기 | 간트 범위·반복 필터는 간트 제목으로 이동, 대분류·완료·화면배치는 간트와 달력 공통 조작부에 유지 |
| HOME 대분류가 간트와 달력에 함께 적용된다 | 자동 통과 / 화면 확인 대기 | 달력 최종 렌더 경로에서도 선택 대분류와 완료 표시 조건을 재검증 |
| 새로고침 뒤 업무·체크·완료 이력이 유지된다 | 실기기 대기 | Firestore 영속 캐시·IndexedDB 저장 구현, 로그인 뒤 최종 확인 필요 |
| 로그아웃 상태의 Firestore 읽기/쓰기가 차단된다 | 실제 통과 | 최종 Rules 게시·전파 후 비인증 REST 읽기와 쓰기 모두 HTTP 403 `PERMISSION_DENIED` 재확인 |
| PC 화면에서 핵심 UI가 열린다 | 실제 통과 | 배포 URL의 2026년 9월 HOME·간트·달력·내비게이션 렌더 확인 |
| 360px 모바일·태블릿·1280px PC에서 핵심 조작 가능 | 실기기 대기 | 반응형 규칙 존재, Cloud Browser 파일 선택창 장애로 최종 화면 회귀 미완료 |
| Android·갤럭시탭·iPad에서 홈 화면 설치 후 standalone 실행 | 실기기 대기 | manifest `standalone`, 192/512 아이콘, 서비스 워커·Firebase 모듈 사전 캐시 계약 통과 |
| Google Drive 사진 업로드·다른 기기 열람 | 권한 승인 후 실기기 대기 | `drive.file` 최소 권한과 Drive 파일 ID 저장 구현. Drive API 활성화 확인 완료; 앱 로그인 뒤 최초 1회 Google 권한 승인 필요 |
| 체크리스트의 관련 공문 제목 저장·검색 | 자동 통과 / 화면 확인 대기 | 선택 입력값을 해당 업무 문서에만 저장하고 새로고침 복원·업무목록 검색·CSV 내보내기에 포함 |
| 모바일 간트 대분류의 마지막 항목 선택 | 자동 통과 / 실기기 확인 대기 | 메뉴를 보이는 화면에 고정하고 내부 세로 스크롤·다른 카드보다 높은 층 순서 적용 |
| 모바일 업무 작성·수정 하단 버튼 접근 | 자동 통과 / 실기기 확인 대기 | 동적 뷰포트 높이(`dvh`)에 맞춰 본문만 스크롤하고 하단 버튼 영역 고정 |
| 모바일 HOME 전체 구성 | 자동 통과 / 실기기 확인 대기 | 필터·KPI·간트·달력을 한 열 중심으로 재배치하고 달력은 가로 스크롤 적용 |
| 모바일 수행업무·업무 DB 가독성 | 자동 통과 / 실기기 확인 대기 | 데스크톱 표를 카드 목록으로 전환하고 업무명을 카드 전체 폭·단어 단위 줄바꿈으로 표시 |
| 모바일 전체 화면 이동 | 자동 통과 / 실기기 확인 대기 | HOME·업무·업무DB·휴일·이력·설정 고정 하단 메뉴 적용 |
| 모바일 변경이력·휴일·설정 | 자동 통과 / 실기기 확인 대기 | 표·입력·설정 카드를 모바일 한 열 구조로 전환 |
| 모바일 체크리스트 완료 버튼 | 자동 통과 / 실기기 확인 대기 | 팝업을 하단 메뉴 위에 표시하고 ‘내용 수정·업무 완료’ 버튼을 항상 노출 |
| 완료 상태 저장·복원 | 자동 통과 / 실기기 확인 대기 | 상태와 실제 완료일을 해당 업무 문서에 저장하고 새로고침 후 유지 |
| 날짜 8자리 직접 입력 | 자동 통과 / 실기기 확인 대기 | `20260910`을 `2026-09-10`으로 변환하고 존재하지 않는 날짜는 정상값으로 인정하지 않음; 달력 선택도 유지 |
| 반복업무 조건부 입력과 실시 월 저장 | 자동 통과 / 실기기 확인 대기 | 반복 체크 전 세부항목 숨김, 주간 7요일 정렬, 월간 이상 1~12월 직접 선택·반복 생성·클라우드 복원 검사 통과 |
| 팝업 바깥 클릭 시 작성 내용 보존 | 자동 통과 / 실기기 확인 대기 | 모든 팝업의 배경 클릭 닫기 경로 제거; 닫기·취소 버튼으로만 종료 |
| 설정/백업 반복 진입 성능 | 자동 통과 / 실기기 확인 대기 | HOME의 불필요한 설정 렌더 제거, 설정 버튼의 누적 pointer 이벤트 제거, 테마 카드 재생성 방지 |
| 모바일 팝업 정리 | 자동 통과 / 실기기 확인 대기 | 팝업 중 하단 메뉴를 숨기고 짧은 체크리스트의 불필요한 전체화면 빈 공간 제거 |
| 동일 화면 중복 기능 제거 | 자동 통과 / 화면 확인 대기 | HOME 대분류·화면배치 조작을 각각 한 곳으로 통일하고, 완료 처리를 체크리스트 창 한 곳으로 통일했으며 설정의 중복 이동 버튼 제거 |
| V22 A구조·B색상 디자인 | 자동 통과 / 화면 확인 대기 | PC 왼쪽 네이비 내비게이션, HOME 오늘 요약·월간 완료율·가까운 일정, 네이비 카드 계층과 모바일 한 열·하단 메뉴 조합 적용. 요약 카드 대비 고정 및 업무관리 로고의 HOME 이동 포함 |

개발 검증 명령은 아래와 같다. 이 명령은 유지보수 검사용이며 회사 PC나 최종 사용 기기에 Node.js 설치를 요구하지 않는다.

```sh
node tests/v10-regression.test.mjs
node tests/cloud-state-roundtrip.test.mjs
node tests/cloud-contract.test.mjs
```
